#!/usr/bin/env python3
"""that takes a string str1 and a string str2 as arguments and returns
        a given string from user.given string from user.

    Args:
        str1 (str): given first string from user.
        str2 (str): given second string from user.

    Returns:
        str: return a given string from user.
    """


def concat(str1: str, str2: str) -> str:
    """that takes a string str1 and a string str2 as arguments and returns
        a given string from user.given string from user.

    Args:
        str1 (str): given first string from user.
        str2 (str): given second string from user.

    Returns:
        str: return a given string from user.
    """
    return str1 + str2
